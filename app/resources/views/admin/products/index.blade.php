@extends('layouts.admin')
@section('title','Products')
@section('content')
<div class="admin-page-head"><div><span class="eyebrow">Catalog</span><h1>Products</h1><p>Manage prices, availability and inventory.</p></div><a class="button" href="{{ route('admin.products.create') }}">+ Add product</a></div><form class="admin-filters" method="get"><input name="q" value="{{ request('q') }}" placeholder="Search name or SKU"><label><input type="checkbox" name="low_stock" value="1" @checked(request('low_stock')) onchange="this.form.submit()"> Low stock only</label><button class="button button--secondary" type="submit">Search</button></form><section class="admin-panel"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th></th></tr></thead><tbody>@forelse($products as $product)<tr><td><div class="admin-product-cell"><img src="{{ $product->imageUrl() }}" alt=""><span><strong>{{ $product->name }}</strong><small>{{ $product->sku }}</small></span></div></td><td>{{ $product->category?->name ?? '—' }}</td><td>AED {{ number_format((float)$product->price,2) }}</td><td><span class="stock-pill {{ $product->stock <= $product->low_stock_threshold ? 'low':'' }}">{{ $product->stock }}</span></td><td><span class="status status--{{ $product->is_active?'completed':'cancelled' }}">{{ $product->is_active?'Active':'Archived' }}</span></td><td><a class="table-action" href="{{ route('admin.products.edit',$product) }}">Edit</a></td></tr>@empty<tr><td colspan="6">No products match your search.</td></tr>@endforelse</tbody></table></div>{{ $products->links() }}</section>
@endsection
