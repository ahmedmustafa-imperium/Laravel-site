@extends('layouts.admin')
@section('title','Categories')
@section('content')
<div class="admin-page-head"><div><span class="eyebrow">Catalog structure</span><h1>Categories</h1><p>Organize the storefront and mega menu.</p></div><a class="button" href="{{ route('admin.categories.create') }}">+ Add category</a></div><section class="admin-panel"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Category</th><th>Parent</th><th>Products</th><th>Order</th><th>Status</th><th></th></tr></thead><tbody>@foreach($categories as $category)<tr><td><div class="admin-product-cell"><img src="{{ $category->imageUrl() }}" alt=""><span><strong>{{ $category->name }}</strong><small>/collections/{{ $category->slug }}</small></span></div></td><td>{{ $category->parent?->name ?? 'Top level' }}</td><td>{{ $category->products_count }}</td><td>{{ $category->sort_order }}</td><td><span class="status status--{{ $category->is_active?'completed':'cancelled' }}">{{ $category->is_active?'Active':'Archived' }}</span></td><td><a class="table-action" href="{{ route('admin.categories.edit',$category) }}">Edit</a></td></tr>@endforeach</tbody></table></div>{{ $categories->links() }}</section>
@endsection
