@extends('layouts.admin')
@section('title','Orders')
@section('content')
<div class="admin-page-head"><div><span class="eyebrow">Order management</span><h1>Orders</h1><p>Review payments, fulfilment and customer details.</p></div></div><form class="admin-filters" method="get"><input name="q" value="{{ request('q') }}" placeholder="Order, customer or email"><select name="status"><option value="">All statuses</option>@foreach(['pending','processing','shipped','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="button button--secondary" type="submit">Filter</button></form><section class="admin-panel"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Fulfilment</th><th>Payment</th><th>Total</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->number }}</strong><small>{{ $order->created_at->format('d M Y, H:i') }}</small></td><td>{{ $order->customer_name }}<small>{{ $order->customer_email }}</small></td><td><span class="status status--{{ $order->status }}">{{ str($order->status)->headline() }}</span></td><td><span class="status status--{{ $order->payment_status }}">{{ str($order->payment_status)->headline() }}</span><small>{{ str($order->payment_method)->headline() }}</small></td><td><strong>AED {{ number_format((float)$order->total,2) }}</strong></td><td><a class="table-action" href="{{ route('admin.orders.show',$order) }}">Open</a></td></tr>@empty<tr><td colspan="6">No orders found.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</section>
@endsection
